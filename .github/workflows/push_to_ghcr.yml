# Copyright 2025 Lincoln Institute of Land Policy
# SPDX-License-Identifier: Apache-2.0

name: Push To GHCR

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group:
    ${{ github.workflow }}-${{ github.ref_name }}-${{
    github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  build_and_push:
    name: "build and push geoconnex bulk rdf export image"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and export
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ghcr.io/internetofwater/usgs_monitoring_locations_bulk_rdf:latest
          cache-from: type=gha,scope=usgs_monitoring_locations_bulk_rdf
          platforms: linux/amd64,linux/arm64
          cache-to: type=gha,mode=max,scope=usgs_monitoring_locations_bulk_rdf
